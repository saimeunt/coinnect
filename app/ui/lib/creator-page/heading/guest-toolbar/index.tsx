import SignUpButton from '@/app/ui/lib/creator-page/heading/guest-toolbar/sign-up-button';

const GuestToolbar = ({ slug }: { slug: string }) => <SignUpButton slug={slug} />;

export default GuestToolbar;
